spring:
  datasource:
    url: jdbc:mysql://localhost:3306/teamfresh
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ggozlo
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    database: mysql


logging.level:
  org.hibernate.SQL: debug
